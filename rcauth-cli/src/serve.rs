use rcauth_core::error::Result;
use tracing::info;

pub async fn run() -> Result<()> {
    info!("Starting authentication server");
    info!("Server not yet implemented");

    // TODO: Implement server startup logic

    Ok(())
}
